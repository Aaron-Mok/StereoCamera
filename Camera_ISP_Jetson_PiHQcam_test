import cv2
import os
import numpy as np
from ISP import *
from camera_utils import *
from conversion_utils import *
# from colour_demosaicing import demosaicing_CFA_Bayer_Menon2007, demosaicing_CFA_Bayer_bilinear
# this is too slow for real time processing, so we use OpenCV's demosaicing instead

# Initialize camera and capture folder
cam_obj, output_im_size_px = initialize_camera(camera_id = 1, image_size = (1920, 1080))
capture_dir = "captures"
os.makedirs(capture_dir, exist_ok=True)
capture_count = 1

while True:
    raw_u8 = capture_raw_image(cam_obj)
    
    # Just to see the 'static' of the raw bytes
    # 1920 * 1.25 = 2400 bytes wide
    cv2.imshow("Raw Byte Stream", raw_u8)

    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cv2.destroyAllWindows()